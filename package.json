{
  "name": "projekat-constellation",
  "version": "1.0.0",
  "description": "This is a project for the social media web application ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-all-chrome-headless": "cypress run --browser chrome --headless --reporter mochawesome",
    "test-all-firefox": "cypress run --browser firefox --reporter mochawesome",
    "test-all-electron": "cypress run --browser electron --headless --reporter mochawesome"
  },
  "author": "Martin Saric",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.4.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
